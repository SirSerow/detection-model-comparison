-r common.txt

# Validated target: Raspberry Pi OS Legacy Lite 64-bit (Bookworm), Python 3.11.
# Phase 1 is deliberately CPU-only: ONNX Runtime FP32 and NCNN FP32.
numpy==2.2.6
opencv-python==4.12.0.88
pycocotools==2.0.11
psutil==7.0.0
onnxruntime==1.29.0
ncnn==1.0.20260114

# PyTorch and detector source packages are a separate, gated phase. NCNN
# conversion uses a pinned pnnx release on the build machine; the Pi needs
# only the ncnn runtime wheel for benchmark execution.
