-r common.txt

# CPU-only Pi 4 stack. Install on-device.
# torch
# torchvision
# onnxruntime
# ncnn
# PyTorch model packages are optional on Pi; exported ONNX/NCNN artifacts can
# be produced on a compatible build machine and copied into weights/.
